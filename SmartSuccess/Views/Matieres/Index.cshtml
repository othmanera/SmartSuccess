@model IEnumerable<SmartSuccess.Models.Matiere>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nom)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prix)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Paiement.Etat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Professeur.Nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nom)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prix)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Paiement.Etat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Professeur.Nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Matiere }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Matiere }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Matiere })
        </td>
    </tr>
}

</table>
